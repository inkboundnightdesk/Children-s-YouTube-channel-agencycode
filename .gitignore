# Build output - generated per video, not source
build/

# Fetched policy snapshots - dated working copies, regenerated by --refresh
compliance/_fetched/

__pycache__/
*.py[cod]
.DS_Store
.venv/
venv/

# Real clearance documents may be sensitive/contractual. Track deliberately, not by accident.
# Remove this line if you version your evidence (recommended - just make the repo private).
music/evidence/*.pdf
